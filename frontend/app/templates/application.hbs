<nav class="navbar navbar-expand-lg navbar-light bg-light">
  {{#link-to "create" class="navbar-brand"}}
    <img class="logo d-inline-block align-top" src="/logo.png" alt="Strawpoller Logo">
    Strawpoller
  {{/link-to}}
  {{#if (or socket.publicPolls socket.allPolls)}}
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbar">
      <span class="navbar-toggler-icon"></span>
    </button>
  {{/if}}
  <div id="navbar" class="collapse navbar-collapse">
    <ul class="navbar-nav">
      {{#if socket.publicPolls}}
        <li class="nav-item">
          {{#link-to "public" class="nav-link"}}Public Polls{{/link-to}}
        </li>
      {{/if}}
      {{#if socket.allPolls}}
        <li class="nav-item">
          {{#link-to "all" class="nav-link"}}All Polls{{/link-to}}
        </li>
      {{/if}}
    </ul>
  </div>
</nav>
<br>
<div class="container bg-light text-center">
  <br>
  {{#if socket.isHandshook}}
    {{outlet}}
  {{else}}
    <h2 class="text-center">Connecting...</h2>
  {{/if}}
  <br>
</div>
<footer class="container pt-4 my-md-5 pt-md-5 border-top">
  <div class="row text-center">
    <div class="col-12 col-md">
      <div class="footer-logo">
        <img class="logo" src="/logo.png"><span>Strawpoller</span>
      </div>
      <p class="text-muted">A website by
        <a href="https://deanlevinson.com.au" target="_blank">Dean Levinson</a>
        &ndash; source on <a href="https://github.com/deanylev/strawpoller" target="_blank">GitHub</a>
      </p>
    </div>
  </div>
</footer>
